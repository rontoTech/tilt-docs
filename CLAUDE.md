# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The canonical guide is @AGENTS.md. Key points: push to main deploys the live site; navigation exists only in `docs.json` (new pages must be registered there); several pages are known-stale (sdk-quickstart, deployments.mdx, politician pages) — verify addresses against `bowstring-ui/src/lib/contracts.ts` before propagating them. The workspace-level guide (if present) is `../AGENTS.md`.
